"""Gateway configuration: `settings` (pydantic, `.env` floor) and `runtime_config` (the overlay).

Packaged from the two top-level modules `config/settings.py` / `config/runtime_config.py`
(CLEANUP_PLAN step 3.6). `.env` still lives at the repo root; see
`config/settings.py` for how it is found.
"""
